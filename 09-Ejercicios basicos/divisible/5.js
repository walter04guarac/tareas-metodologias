'use strict';

let numero =+prompt('Ingrese un número:');

if (numero % 5 === 0) {
    alert(numero + ' El numero es divisible para 5');
} else {
    alert(numero + ' El numero no es divisible para 5');
}
